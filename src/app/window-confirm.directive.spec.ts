import { WindowConfirmDirective } from './window-confirm.directive';

describe('WindowConfirmDirective', () => {
  it('should create an instance', () => {
    const directive = new WindowConfirmDirective();
    expect(directive).toBeTruthy();
  });
});
